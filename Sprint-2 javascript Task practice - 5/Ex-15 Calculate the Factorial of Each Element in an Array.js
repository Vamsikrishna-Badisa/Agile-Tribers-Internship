import math

# Given array of numbers
numbers = [3, 4, 5, 6]

# Loop through each number in the array
for num in numbers:
    factorial = math.factorial(num)  # Calculate factorial
    print(f"Factorial of {num} is {factorial}")
